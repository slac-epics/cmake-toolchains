# CMake Toolchains for SLAC Buildroot Targets

These CMake toolchains are for use with SLAC's buildroot distribution, and potentially other targets in the future.

Use these to cross compile CMake software for the desired target.